import BlogPanel from "@/components/BlogPanel";

export default function Learn() {
  return (
    <main className='w-full min-h-screen items-center justify-between align-center'>
       <BlogPanel/>
    </main>
  );
};
